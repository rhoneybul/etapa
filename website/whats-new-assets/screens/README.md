Drop screenshots here. The page at /whats-new.html currently renders styled placeholders that describe which shot goes in each slot; once a real PNG lands at the filename below, swap the `data-placeholder="1"` / `data-caption="…"` attributes in whats-new.html for a real `<img src="…">` tag (commented skeleton is already in the HTML next to each slot).

Required for the 24 Apr 2026 entry:
- home-screen.png        — Home with Today hero + Tomorrow preview + week strip
- session-breakdown.png  — Activity Detail with the "How to do this session" panel
- plan-picker.png        — One of the three intake questions, mid-flow

Sizing: tall portrait screenshots from the device (iPhone or Android) — the frame has aspect-ratio 9:19.5. Anything roughly iPhone-shaped will fit cleanly.
